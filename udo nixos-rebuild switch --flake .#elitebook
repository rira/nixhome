[1mdiff --git a/modules/hyprland.nix b/modules/hyprland.nix[m
[1mindex dc9a27a..41ba3a8 100644[m
[1m--- a/modules/hyprland.nix[m
[1m+++ b/modules/hyprland.nix[m
[36m@@ -72,12 +72,13 @@[m [min[m
       gtk-theme-name=Adwaita-dark[m
     '';[m
 [m
[31m-    # Rofi configuration with "fancy" theme[m
[32m+[m[32m    # Rofi configuration with "fancy" theme & window switcher[m
     environment.etc."xdg/rofi/config.rasi".text = ''[m
       configuration {[m
[31m-        modi: "drun,run";[m
[32m+[m[32m        modi: "drun,run,window";[m
         show-icons: true;[m
         display-drun: "Apps";[m
[32m+[m[32m        display-window: "Windows";[m
         drun-display-format: "{name}";[m
       }[m
 [m
[36m@@ -134,7 +135,7 @@[m [min[m
       exec-once = trayscale --hide-window[m
 [m
       # Clamshell mode at login: check if lid is already closed when booting docked[m
[31m-      exec-once = sh -c "if grep -q closed /proc/acpi/button/lid/*/state 2>/dev/null; then hyprctl keyword monitor 'eDP-1, disable'; fi"[m
[32m+[m[32m      exec-once = sh -c "sleep 1 && if grep -q closed /proc/acpi/button/lid/*/state 2>/dev/null; then hyprctl keyword monitor 'eDP-1, disable'; fi"[m
 [m
       # --- Monitor Configuration ---[m
       ${lib.concatMapStringsSep "\n" (m: "monitor = ${m}") cfg.monitors}[m
[36m@@ -175,6 +176,9 @@[m [min[m
       # Keybindings (Super = Windows key)[m
       $mainMod = SUPER[m
 [m
[32m+[m[32m      # Window switcher menu (Alt + Tab)[m
[32m+[m[32m      bind = ALT, Tab, exec, rofi -show window[m
[32m+[m
       # Screenshot to clipboard[m
       bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" - | wl-copy --type image/png[m
 [m
[36m@@ -249,5 +253,10 @@[m [min[m
       bindm = $mainMod, mouse:272, movewindow[m
       bindm = $mainMod, mouse:273, resizewindow[m
     '';[m
[32m+[m
[32m+[m[32m    # Kitty base configuration (inaktiverar paste protection)[m
[32m+[m[32m    environment.etc."xdg/kitty/kitty.conf".text = ''[m
[32m+[m[32m      paste_actions quote-urls-at-prompt,replace-dangerous-control-codes[m
[32m+[m[32m    '';[m
   };[m
 }[m
